# Connect Codex

Open MCP Clients -> Codex.

Use the Copy Codex TOML button and paste it into:

```text
~/.codex/config.toml
```

Install the user helper symlink first if the stable helper path is missing:

```text
~/bin/nala-mcp-core-helper
```
