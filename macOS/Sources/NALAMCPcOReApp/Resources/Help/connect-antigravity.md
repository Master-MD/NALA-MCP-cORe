# Connect Google Antigravity

Antigravity has two separate areas:

1. Permissions
2. Real MCP server config

The permission screen only allows or denies tools. It does not register the server.

Put the copied config here:

```text
~/.gemini/antigravity/mcp_config.json
```
