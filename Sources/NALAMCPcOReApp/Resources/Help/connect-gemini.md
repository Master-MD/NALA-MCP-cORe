# Connect Gemini CLI

Open MCP Clients -> Gemini CLI.

Copy the JSON config into:

```text
~/.gemini/settings.json
```

Important:

```text
~/.gemini/GEMINI.md is for rules and instructions.
It is not the MCP server config.
```
